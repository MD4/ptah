{
  "name": "@ptah/lib-logger",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist/**"],
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "jest": {
    "preset": "jest-presets/jest/node"
  },
  "devDependencies": {
    "@ptah/config-jest": "workspace:*",
    "@ptah/config-ts": "workspace:*",
    "@types/node": "^22.9.4",
    "tsup": "^7.2.0",
    "typescript": "^5.7.2"
  }
}
